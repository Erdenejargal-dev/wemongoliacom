import { GuideReviewsManager } from '@/components/guide-dashboard/GuideReviewsManager'

export const metadata = { title: 'Reviews — Guide Portal' }

export default function GuideReviewsPage() {
  return <GuideReviewsManager />
}

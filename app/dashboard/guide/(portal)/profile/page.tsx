import { GuideProfileEditor } from '@/components/guide-dashboard/GuideProfileEditor'

export const metadata = { title: 'Edit Profile — Guide Portal' }

export default function GuideProfilePage() {
  return <GuideProfileEditor />
}
